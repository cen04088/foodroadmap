# 프론트엔드 (Next.js) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 출발지/도착지를 검색해 경로상 방송 맛집을 지도+리스트로 보여주는 Next.js 단일 페이지 웹앱을 만든다.

**Architecture:** Next.js App Router 단일 페이지, 클라이언트 컴포넌트가 브라우저에서 백엔드(`GET /api/route-restaurants`)를 직접 호출한다(프록시 없음). 카카오맵 JS SDK를 브라우저에서 로드해 장소 검색(Places)과 지도 렌더링에 사용한다. 순수 로직(포맷터, API 클라이언트)만 자동 테스트하고, SDK/UI는 브라우저 수동 확인으로 검증한다.

**Tech Stack:** Next.js(App Router, TypeScript), Tailwind CSS, 카카오맵 JS SDK, Vitest(단위 테스트), FastAPI(백엔드, CORS 추가만 이 계획에 포함)

**Spec:** [docs/superpowers/specs/2026-09-03-frontend-design.md](../specs/2026-09-03-frontend-design.md) — 이 계획은 그 설계 문서 전체(§2~§8)를 구현한다. 상위 스펙 [2026-09-01-route-restaurant-service-design.md](../specs/2026-09-01-route-restaurant-service-design.md) §2.6, §5도 참고한다.

## Global Constraints

- 단일 페이지. 클라이언트가 백엔드를 직접 호출한다(Next.js 라우트 핸들러 프록시 없음).
- 환경변수: 프론트엔드 `NEXT_PUBLIC_KAKAO_JS_KEY`(카카오맵 JS SDK 키), `NEXT_PUBLIC_API_BASE_URL`(백엔드 주소, 로컬 `http://localhost:8000`). 백엔드 `FRONTEND_ORIGINS`(CORS 허용 오리진, 콤마 구분, 기본값 `http://localhost:3000`).
- 실제 API 키는 어떤 파일에도 커밋하지 않는다. `.env.local`은 `.gitignore`에 포함되어야 한다.
- 방송프로그램 필터는 상위 스펙 §2.2의 14개 프로그램을 프론트엔드에 고정 목록으로 넣고, **표시명**(예: "또간집")을 그대로 `broadcast` 쿼리 파라미터 값으로 보낸다. 백엔드의 `broadcast` 필터는 슬러그와 표시명을 둘 다 매칭하도록 이미 구현되어 있다.
- 업종(`category`) 필터는 자유 텍스트 입력이며, 백엔드는 정확히 일치하는 값만 매칭한다(대소문자/부분일치 없음).
- 반경(`radius_km`) 조정 UI는 포함하지 않는다 — 상위 스펙 §8에서 명시적으로 MVP 범위 밖.
- 방송/업종 필터가 바뀌면 같은 출발/도착으로 **매번 재요청**한다. 클라이언트 사이드 캐싱/재필터링 최적화는 하지 않는다(YAGNI).
- 에러 메시지 매핑: `ApiError.status === 0`(네트워크 실패) → "서버에 연결할 수 없습니다", `500` → "일시적인 오류입니다, 잠시 후 다시 시도해주세요", `502` → "경로를 가져오지 못했습니다, 다시 시도해주세요". 매칭된 맛집이 0건인 것은 에러가 아니라 빈 상태("이 경로 근처엔 방송 맛집이 없어요")로 표시한다.
- 자동 테스트 대상은 순수 로직(`lib/format.ts`, `lib/api.ts`)뿐이다. 카카오 SDK 연동, React 컴포넌트는 자동 테스트를 만들지 않고 개발 서버에서 브라우저로 직접 확인한다(설계 문서 §8).

---

## 프로젝트 구조 (신규 `frontend/` 디렉토리, 기존 `backend/`/`docs/`와 나란히)

```
frontend/
  app/
    page.tsx                  # 메인 페이지 (Task 9)
    layout.tsx, globals.css   # create-next-app 기본 생성 (Task 2)
  components/
    SearchForm.tsx             # 신규 (Task 5)
    FilterBar.tsx                # 신규 (Task 6)
    MapView.tsx                   # 신규 (Task 7)
    RestaurantList.tsx             # 신규 (Task 8)
    RestaurantCard.tsx              # 신규 (Task 8)
  lib/
    format.ts                        # 신규 (Task 3)
    api.ts                             # 신규 (Task 3)
    kakaoMap.ts                         # 신규 (Task 4)
    kakaoPlaces.ts                       # 신규 (Task 4)
  types/
    kakao.d.ts                            # 신규 (Task 4)
  vitest.config.ts                          # 신규 (Task 3)
  .env.local.example                         # 신규 (Task 2)
```

백엔드 변경: `backend/app/api/main.py`에 CORS 미들웨어 추가 (Task 1).

---

### Task 1: 백엔드 CORS 미들웨어

**Files:**
- Modify: `backend/app/api/main.py`
- Test: `backend/tests/test_api_cors.py`

**Interfaces:**
- Consumes: 없음 (기존 `app.api.routes.router` 그대로 사용)
- Produces: 없음 (다른 태스크가 이 변경에 의존하지 않음 — 프론트엔드가 브라우저에서 백엔드를 호출할 때 필요한 응답 헤더만 추가됨)

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/tests/test_api_cors.py`:
```python
from fastapi.testclient import TestClient

from app.api.main import app


def test_cors_allows_configured_frontend_origin():
    client = TestClient(app)
    response = client.get(
        "/api/route-restaurants",
        params={"origin": "not-a-coordinate", "destination": "37.6,127.1"},
        headers={"Origin": "http://localhost:3000"},
    )
    assert response.headers["access-control-allow-origin"] == "http://localhost:3000"


def test_cors_rejects_unconfigured_origin():
    client = TestClient(app)
    response = client.get(
        "/api/route-restaurants",
        params={"origin": "not-a-coordinate", "destination": "37.6,127.1"},
        headers={"Origin": "http://evil.example.com"},
    )
    assert "access-control-allow-origin" not in response.headers
```

이 테스트는 `origin` 쿼리 파라미터를 일부러 잘못된 값(`"not-a-coordinate"`)으로 보내 400 응답을 유도한다 — CORS 미들웨어는 응답 상태코드와 무관하게 헤더를 붙이므로, 유효한 좌표나 카카오 API 모킹 없이도 CORS 동작만 가볍게 검증할 수 있다.

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `cd backend && pytest tests/test_api_cors.py -v`
Expected: FAIL — `KeyError: 'access-control-allow-origin'` (헤더가 아직 없음)

- [ ] **Step 3: 구현**

`backend/app/api/main.py`를 다음으로 교체한다:
```python
import os

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.api.routes import router

app = FastAPI(title="foodmap route-restaurants API")

_allowed_origins = [
    origin.strip()
    for origin in os.environ.get("FRONTEND_ORIGINS", "http://localhost:3000").split(",")
]

app.add_middleware(
    CORSMiddleware,
    allow_origins=_allowed_origins,
    allow_methods=["GET"],
    allow_headers=["*"],
)

app.include_router(router)
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

Run: `cd backend && pytest tests/test_api_cors.py -v`
Expected: PASS

- [ ] **Step 5: 전체 백엔드 테스트 스위트 실행**

Run: `cd backend && pytest -v`
Expected: 기존 테스트 전부 + 새 CORS 테스트 2개 모두 PASS, 리그레션 없음

- [ ] **Step 6: 커밋**

```bash
git add backend/app/api/main.py backend/tests/test_api_cors.py
git commit -m "feat: add CORS middleware for frontend origin"
```

---

### Task 2: Next.js 프로젝트 스캐폴드 + 환경변수

**Files:**
- Create: `frontend/` 전체 (create-next-app 생성)
- Create: `frontend/.env.local.example`

**Interfaces:**
- Consumes: 없음
- Produces: `frontend/` 프로젝트 골격 (이후 모든 태스크가 이 디렉토리 안에서 작업), `NEXT_PUBLIC_KAKAO_JS_KEY`/`NEXT_PUBLIC_API_BASE_URL` 환경변수 계약

- [ ] **Step 1: Next.js 프로젝트 생성**

저장소 루트(`C:\Users\Minjun\Desktop\foodmap`)에서:
```bash
npx create-next-app@latest frontend --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm
```

대화형 프롬프트가 뜨면 다음으로 답한다: TypeScript = Yes, ESLint = Yes, Tailwind CSS = Yes, `src/` 디렉토리 사용 = No, App Router = Yes, import alias 커스터마이즈 = Yes → `@/*`, Turbopack 사용 여부를 물으면 기본값(Enter) 그대로 진행.

- [ ] **Step 2: 빌드 확인**

Run: `cd frontend && npm run build`
Expected: 기본 템플릿이 에러 없이 빌드됨

- [ ] **Step 3: .gitignore에 env 파일이 무시되는지 확인**

Run: `cd frontend && grep -n "env" .gitignore` (Windows PowerShell이면 `Select-String env .gitignore`)
Expected: `.env*.local` 패턴이 존재함 (create-next-app 기본 템플릿에 포함되어 있음). 없다면 `.gitignore`에 `.env*.local` 줄을 직접 추가한다.

- [ ] **Step 4: 환경변수 예시 파일 작성**

`frontend/.env.local.example`:
```
NEXT_PUBLIC_KAKAO_JS_KEY=
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

- [ ] **Step 5: 실제 로컬 환경변수 파일 생성 (커밋 안 됨)**

```bash
cd frontend
cp .env.local.example .env.local
```
`frontend/.env.local`을 열어 `NEXT_PUBLIC_KAKAO_JS_KEY=`에 실제 카카오맵 JS SDK 앱 키를 채워넣는다. 이 파일은 `.gitignore`로 무시되므로 git에 올라가지 않는다 — **절대 실제 키 값을 커밋하지 않는다.**

- [ ] **Step 6: 커밋 전 .env.local이 스테이징되지 않았는지 확인**

Run: `git status --short frontend/`
Expected: `frontend/.env.local`이 목록에 없어야 함(무시됨). `frontend/.env.local.example`은 나타나야 함.

- [ ] **Step 7: 커밋**

```bash
git add frontend/
git commit -m "feat: scaffold Next.js frontend project"
```

---

### Task 3: 코어 유틸리티 — 포맷터 + API 클라이언트 (Vitest 셋업 포함)

**Files:**
- Create: `frontend/lib/format.ts`
- Create: `frontend/lib/api.ts`
- Create: `frontend/vitest.config.ts`
- Test: `frontend/lib/format.test.ts`
- Test: `frontend/lib/api.test.ts`
- Modify: `frontend/package.json` (scripts에 `test` 추가)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `formatDuration(totalSeconds: number): string`, `formatDistance(km: number): string` (`lib/format.ts`)
  - `RoutePoint`, `RestaurantResult`, `RouteRestaurantsResponse`, `ApiError`, `FetchRouteRestaurantsParams`, `fetchRouteRestaurants(params, baseUrl?): Promise<RouteRestaurantsResponse>` (`lib/api.ts`) — 이후 모든 컴포넌트 태스크가 이 타입들을 그대로 가져다 쓴다

- [ ] **Step 1: Vitest 설치**

Run: `cd frontend && npm install --save-dev vitest`

- [ ] **Step 2: package.json에 test 스크립트 추가**

`frontend/package.json`의 `"scripts"` 객체에 `"test": "vitest run"` 항목을 추가한다 (기존 `dev`/`build`/`start`/`lint` 스크립트는 그대로 둔다).

- [ ] **Step 3: Vitest 설정 파일 작성**

`frontend/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
  },
});
```

- [ ] **Step 4: 실패하는 테스트 작성 — 포맷터**

`frontend/lib/format.test.ts`:
```ts
import { describe, expect, it } from "vitest";
import { formatDuration, formatDistance } from "./format";

describe("formatDuration", () => {
  it("formats under an hour as minutes only", () => {
    expect(formatDuration(0)).toBe("0분");
    expect(formatDuration(59)).toBe("0분");
    expect(formatDuration(60)).toBe("1분");
    expect(formatDuration(45 * 60)).toBe("45분");
  });

  it("formats an hour or more as hours and minutes", () => {
    expect(formatDuration(60 * 60)).toBe("1시간 0분");
    expect(formatDuration(80 * 60)).toBe("1시간 20분");
    expect(formatDuration(125 * 60)).toBe("2시간 5분");
  });
});

describe("formatDistance", () => {
  it("rounds to one decimal place and appends km", () => {
    expect(formatDistance(0)).toBe("0.0km");
    expect(formatDistance(1.234)).toBe("1.2km");
    expect(formatDistance(1.98)).toBe("2.0km");
  });
});
```

- [ ] **Step 5: 테스트 실행 → 실패 확인**

Run: `cd frontend && npx vitest run lib/format.test.ts`
Expected: FAIL — `Cannot find module './format'`

- [ ] **Step 6: 포맷터 구현**

`frontend/lib/format.ts`:
```ts
export function formatDuration(totalSeconds: number): string {
  const totalMinutes = Math.floor(totalSeconds / 60);
  const hours = Math.floor(totalMinutes / 60);
  const minutes = totalMinutes % 60;

  if (hours === 0) {
    return `${minutes}분`;
  }
  return `${hours}시간 ${minutes}분`;
}

export function formatDistance(km: number): string {
  return `${km.toFixed(1)}km`;
}
```

- [ ] **Step 7: 테스트 실행 → 통과 확인**

Run: `cd frontend && npx vitest run lib/format.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 8: 실패하는 테스트 작성 — API 클라이언트**

`frontend/lib/api.test.ts`:
```ts
import { afterEach, describe, expect, it, vi } from "vitest";
import { ApiError, fetchRouteRestaurants } from "./api";

const FAKE_RESPONSE = {
  route: { total_distance_m: 15000, total_duration_sec: 1200, points: [] },
  restaurants: [],
};

afterEach(() => {
  vi.unstubAllGlobals();
});

describe("fetchRouteRestaurants", () => {
  it("builds the query string and returns the parsed JSON on success", async () => {
    const fetchMock = vi.fn().mockResolvedValue({
      ok: true,
      status: 200,
      json: async () => FAKE_RESPONSE,
    });
    vi.stubGlobal("fetch", fetchMock);

    const result = await fetchRouteRestaurants(
      {
        originLat: 37.5665,
        originLng: 126.978,
        destinationLat: 37.4979,
        destinationLng: 127.0276,
        broadcast: "또간집",
        category: "한식",
      },
      "http://localhost:8000"
    );

    expect(result).toEqual(FAKE_RESPONSE);

    const calledUrl = new URL(fetchMock.mock.calls[0][0] as string);
    expect(calledUrl.origin + calledUrl.pathname).toBe("http://localhost:8000/api/route-restaurants");
    expect(calledUrl.searchParams.get("origin")).toBe("37.5665,126.978");
    expect(calledUrl.searchParams.get("destination")).toBe("37.4979,127.0276");
    expect(calledUrl.searchParams.get("broadcast")).toBe("또간집");
    expect(calledUrl.searchParams.get("category")).toBe("한식");
  });

  it("throws ApiError with the response status on a non-ok response", async () => {
    const fetchMock = vi.fn().mockResolvedValue({
      ok: false,
      status: 502,
      json: async () => ({}),
    });
    vi.stubGlobal("fetch", fetchMock);

    const params = { originLat: 0, originLng: 0, destinationLat: 0, destinationLng: 0 };
    let caught: unknown;
    try {
      await fetchRouteRestaurants(params, "http://localhost:8000");
    } catch (err) {
      caught = err;
    }

    expect(caught).toBeInstanceOf(ApiError);
    expect((caught as ApiError).status).toBe(502);
  });

  it("throws ApiError with status 0 when the network request itself fails", async () => {
    const fetchMock = vi.fn().mockRejectedValue(new TypeError("network down"));
    vi.stubGlobal("fetch", fetchMock);

    const params = { originLat: 0, originLng: 0, destinationLat: 0, destinationLng: 0 };
    let caught: unknown;
    try {
      await fetchRouteRestaurants(params, "http://localhost:8000");
    } catch (err) {
      caught = err;
    }

    expect(caught).toBeInstanceOf(ApiError);
    expect((caught as ApiError).status).toBe(0);
  });
});
```

- [ ] **Step 9: 테스트 실행 → 실패 확인**

Run: `cd frontend && npx vitest run lib/api.test.ts`
Expected: FAIL — `Cannot find module './api'`

- [ ] **Step 10: API 클라이언트 구현**

`frontend/lib/api.ts`:
```ts
export interface RoutePoint {
  lat: number;
  lng: number;
  cumulative_distance_m: number;
  cumulative_time_sec: number;
}

export interface RestaurantResult {
  id: string;
  name: string;
  category: string | null;
  address: string | null;
  latitude: number;
  longitude: number;
  phone: string | null;
  hours: string | null;
  distance_from_route_km: number;
  cumulative_time_sec: number;
  broadcasts: string[];
}

export interface RouteRestaurantsResponse {
  route: {
    total_distance_m: number;
    total_duration_sec: number;
    points: RoutePoint[];
  };
  restaurants: RestaurantResult[];
}

export class ApiError extends Error {
  status: number;

  constructor(status: number, message: string) {
    super(message);
    this.name = "ApiError";
    this.status = status;
  }
}

export interface FetchRouteRestaurantsParams {
  originLat: number;
  originLng: number;
  destinationLat: number;
  destinationLng: number;
  broadcast?: string;
  category?: string;
}

export async function fetchRouteRestaurants(
  params: FetchRouteRestaurantsParams,
  baseUrl: string = process.env.NEXT_PUBLIC_API_BASE_URL ?? ""
): Promise<RouteRestaurantsResponse> {
  const query = new URLSearchParams({
    origin: `${params.originLat},${params.originLng}`,
    destination: `${params.destinationLat},${params.destinationLng}`,
  });
  if (params.broadcast) query.set("broadcast", params.broadcast);
  if (params.category) query.set("category", params.category);

  let response: Response;
  try {
    response = await fetch(`${baseUrl}/api/route-restaurants?${query.toString()}`);
  } catch {
    throw new ApiError(0, "네트워크 오류: 서버에 연결할 수 없습니다");
  }

  if (!response.ok) {
    throw new ApiError(response.status, `요청 실패: ${response.status}`);
  }

  return response.json() as Promise<RouteRestaurantsResponse>;
}
```

- [ ] **Step 11: 테스트 실행 → 통과 확인**

Run: `cd frontend && npx vitest run lib/api.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 12: 전체 Vitest 스위트 실행**

Run: `cd frontend && npm test`
Expected: 모든 테스트(포맷터 3개 + API 클라이언트 3개) PASS

- [ ] **Step 13: 커밋**

```bash
git add frontend/lib/format.ts frontend/lib/format.test.ts frontend/lib/api.ts frontend/lib/api.test.ts frontend/vitest.config.ts frontend/package.json frontend/package-lock.json
git commit -m "feat: add format utilities and API client with tests"
```

---

### Task 4: 카카오맵 SDK 통합 (타입 선언 + 로더 + 장소 검색)

**Files:**
- Create: `frontend/types/kakao.d.ts`
- Create: `frontend/lib/kakaoMap.ts`
- Create: `frontend/lib/kakaoPlaces.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `loadKakaoMapsSdk(): Promise<any>` (`lib/kakaoMap.ts`) — 카카오맵 SDK를 한 번만 로드하고 전역 `kakao` 객체를 반환. 여러 컴포넌트가 동시에 호출해도 안전(캐시됨)
  - `PlaceResult { name: string; address: string; lat: number; lng: number }`, `searchPlaces(kakao: any, keyword: string): Promise<PlaceResult[]>` (`lib/kakaoPlaces.ts`)

카카오 SDK는 타입이 없는 외부 스크립트이므로, 이 태스크는 SDK 경계에서만 `any`를 쓰고 그 바깥(다른 컴포넌트가 보는 함수 시그니처)은 구체적인 타입으로 감싼다. 자동 테스트는 만들지 않는다(브라우저의 실제 `window.kakao` 없이는 의미있게 검증할 수 없음, 설계 문서 §8) — 대신 TypeScript 컴파일이 통과하는지로 검증하고, 실제 동작 확인은 이 함수들을 사용하는 Task 5(SearchForm)/Task 7(MapView)에서 브라우저로 한다.

- [ ] **Step 1: 카카오 SDK 전역 타입 선언**

`frontend/types/kakao.d.ts`:
```ts
export {};

declare global {
  interface Window {
    kakao: any;
  }
}
```

- [ ] **Step 2: SDK 로더 구현**

`frontend/lib/kakaoMap.ts`:
```ts
// 여러 컴포넌트가 동시에 호출해도 스크립트 태그를 한 번만 삽입하도록 캐시한다.
let sdkPromise: Promise<any> | null = null;

export function loadKakaoMapsSdk(): Promise<any> {
  if (typeof window === "undefined") {
    return Promise.reject(new Error("loadKakaoMapsSdk can only run in the browser"));
  }

  if (window.kakao?.maps) {
    return Promise.resolve(window.kakao);
  }

  if (sdkPromise) {
    return sdkPromise;
  }

  sdkPromise = new Promise((resolve, reject) => {
    const appKey = process.env.NEXT_PUBLIC_KAKAO_JS_KEY;
    if (!appKey) {
      reject(new Error("NEXT_PUBLIC_KAKAO_JS_KEY 환경변수가 설정되지 않았습니다"));
      return;
    }

    const script = document.createElement("script");
    script.src = `https://dapi.kakao.com/v2/maps/sdk.js?appkey=${appKey}&autoload=false&libraries=services`;
    script.async = true;
    script.onerror = () => reject(new Error("카카오맵 SDK 로드에 실패했습니다"));
    script.onload = () => {
      window.kakao.maps.load(() => resolve(window.kakao));
    };
    document.head.appendChild(script);
  });

  return sdkPromise;
}
```

- [ ] **Step 3: 장소 검색 래퍼 구현**

`frontend/lib/kakaoPlaces.ts`:
```ts
export interface PlaceResult {
  name: string;
  address: string;
  lat: number;
  lng: number;
}

export function searchPlaces(kakao: any, keyword: string): Promise<PlaceResult[]> {
  return new Promise((resolve, reject) => {
    const places = new kakao.maps.services.Places();
    places.keywordSearch(keyword, (data: any[], status: string) => {
      if (status === kakao.maps.services.Status.OK) {
        resolve(
          data.map((item) => ({
            name: item.place_name,
            address: item.road_address_name || item.address_name,
            lat: Number(item.y),
            lng: Number(item.x),
          }))
        );
        return;
      }
      if (status === kakao.maps.services.Status.ZERO_RESULT) {
        resolve([]);
        return;
      }
      reject(new Error("장소 검색에 실패했습니다"));
    });
  });
}
```

- [ ] **Step 4: 타입 체크**

Run: `cd frontend && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add frontend/types/kakao.d.ts frontend/lib/kakaoMap.ts frontend/lib/kakaoPlaces.ts
git commit -m "feat: add Kakao Maps SDK loader and places search wrapper"
```

---

### Task 5: SearchForm 컴포넌트

**Files:**
- Create: `frontend/components/SearchForm.tsx`

**Interfaces:**
- Consumes: `loadKakaoMapsSdk` (Task 4, `lib/kakaoMap.ts`), `searchPlaces`/`PlaceResult` (Task 4, `lib/kakaoPlaces.ts`)
- Produces: `SelectedPlace { label: string; lat: number; lng: number }`, `SearchFormProps { onOriginSelect?: (place: SelectedPlace) => void; onSearch: (origin: SelectedPlace, destination: SelectedPlace) => void; isLoading: boolean }`, 기본 export `SearchForm`

- [ ] **Step 1: 컴포넌트 구현**

`frontend/components/SearchForm.tsx`:
```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import { loadKakaoMapsSdk } from "../lib/kakaoMap";
import { searchPlaces, type PlaceResult } from "../lib/kakaoPlaces";

export interface SelectedPlace {
  label: string;
  lat: number;
  lng: number;
}

export interface SearchFormProps {
  onOriginSelect?: (place: SelectedPlace) => void;
  onSearch: (origin: SelectedPlace, destination: SelectedPlace) => void;
  isLoading: boolean;
}

function PlaceInput({
  label,
  onSelect,
}: {
  label: string;
  onSelect: (place: SelectedPlace) => void;
}) {
  const [query, setQuery] = useState("");
  const [suggestions, setSuggestions] = useState<PlaceResult[]>([]);
  const [selected, setSelected] = useState<SelectedPlace | null>(null);
  const [hasSearched, setHasSearched] = useState(false);
  const kakaoRef = useRef<any>(null);
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    loadKakaoMapsSdk().then((kakao) => {
      kakaoRef.current = kakao;
    });
  }, []);

  function handleQueryChange(value: string) {
    setQuery(value);
    setSelected(null);
    setHasSearched(false);

    if (debounceRef.current) {
      clearTimeout(debounceRef.current);
    }

    if (!value.trim() || !kakaoRef.current) {
      setSuggestions([]);
      return;
    }

    debounceRef.current = setTimeout(() => {
      searchPlaces(kakaoRef.current, value)
        .then((results) => {
          setSuggestions(results);
          setHasSearched(true);
        })
        .catch(() => {
          setSuggestions([]);
          setHasSearched(true);
        });
    }, 300);
  }

  function handleSelect(place: PlaceResult) {
    const selectedPlace: SelectedPlace = { label: place.name, lat: place.lat, lng: place.lng };
    setSelected(selectedPlace);
    setQuery(place.name);
    setSuggestions([]);
    onSelect(selectedPlace);
  }

  return (
    <div className="relative flex-1">
      <input
        type="text"
        value={query}
        onChange={(e) => handleQueryChange(e.target.value)}
        placeholder={label}
        className="w-full rounded border border-gray-300 px-3 py-2"
      />
      {suggestions.length > 0 && (
        <ul className="absolute z-10 mt-1 w-full rounded border border-gray-200 bg-white shadow-lg">
          {suggestions.map((s) => (
            <li key={`${s.lat}-${s.lng}-${s.name}`}>
              <button
                type="button"
                onClick={() => handleSelect(s)}
                className="block w-full px-3 py-2 text-left hover:bg-gray-100"
              >
                <div className="font-medium">{s.name}</div>
                <div className="text-sm text-gray-500">{s.address}</div>
              </button>
            </li>
          ))}
        </ul>
      )}
      {query && hasSearched && suggestions.length === 0 && !selected && (
        <div className="absolute z-10 mt-1 w-full rounded border border-gray-200 bg-white px-3 py-2 text-sm text-gray-500 shadow-lg">
          검색 결과가 없어요
        </div>
      )}
    </div>
  );
}

export default function SearchForm({ onOriginSelect, onSearch, isLoading }: SearchFormProps) {
  const [origin, setOrigin] = useState<SelectedPlace | null>(null);
  const [destination, setDestination] = useState<SelectedPlace | null>(null);

  function handleOriginSelect(place: SelectedPlace) {
    setOrigin(place);
    onOriginSelect?.(place);
  }

  function handleSubmit() {
    if (origin && destination) {
      onSearch(origin, destination);
    }
  }

  return (
    <div className="flex flex-col gap-2 sm:flex-row">
      <PlaceInput label="출발지" onSelect={handleOriginSelect} />
      <PlaceInput label="도착지" onSelect={setDestination} />
      <button
        type="button"
        disabled={!origin || !destination || isLoading}
        onClick={handleSubmit}
        className="rounded bg-blue-600 px-4 py-2 text-white disabled:bg-gray-300"
      >
        {isLoading ? "검색 중..." : "검색"}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd frontend && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/components/SearchForm.tsx
git commit -m "feat: add SearchForm component with Kakao Places autocomplete"
```

---

### Task 6: FilterBar 컴포넌트

**Files:**
- Create: `frontend/components/FilterBar.tsx`

**Interfaces:**
- Consumes: 없음
- Produces: `Filters { broadcast: string; category: string }`, `BROADCASTS: { value: string; label: string }[]`, `FilterBarProps { filters: Filters; onChange: (filters: Filters) => void }`, 기본 export `FilterBar`

- [ ] **Step 1: 컴포넌트 구현**

`frontend/components/FilterBar.tsx`:
```tsx
"use client";

export interface Filters {
  broadcast: string;
  category: string;
}

export const BROADCASTS: { value: string; label: string }[] = [
  { value: "", label: "전체" },
  { value: "또간집", label: "또간집" },
  { value: "흑백요리사", label: "흑백요리사" },
  { value: "쯔양", label: "쯔양" },
  { value: "쯔양 몇끼", label: "쯔양 몇끼" },
  { value: "먹을텐데", label: "먹을텐데" },
  { value: "전현무계획", label: "전현무계획" },
  { value: "허영만의 백반기행", label: "허영만의 백반기행" },
  { value: "한국인의 밥상", label: "한국인의 밥상" },
  { value: "맛있는 녀석들", label: "맛있는 녀석들" },
  { value: "동네한바퀴", label: "동네한바퀴" },
  { value: "백년가게", label: "백년가게" },
  { value: "비밀이야", label: "비밀이야" },
  { value: "공간 탐닉", label: "공간 탐닉" },
  { value: "김사원세끼", label: "김사원세끼" },
];

export interface FilterBarProps {
  filters: Filters;
  onChange: (filters: Filters) => void;
}

export default function FilterBar({ filters, onChange }: FilterBarProps) {
  return (
    <div className="flex flex-col gap-2 sm:flex-row">
      <select
        value={filters.broadcast}
        onChange={(e) => onChange({ ...filters, broadcast: e.target.value })}
        className="rounded border border-gray-300 px-3 py-2"
      >
        {BROADCASTS.map((b) => (
          <option key={b.value} value={b.value}>
            {b.label}
          </option>
        ))}
      </select>
      <input
        type="text"
        value={filters.category}
        onChange={(e) => onChange({ ...filters, category: e.target.value })}
        placeholder="업종 (예: 한식, 일식)"
        className="rounded border border-gray-300 px-3 py-2"
      />
    </div>
  );
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd frontend && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/components/FilterBar.tsx
git commit -m "feat: add FilterBar component with broadcast dropdown and category input"
```

---

### Task 7: MapView 컴포넌트

**Files:**
- Create: `frontend/components/MapView.tsx`

**Interfaces:**
- Consumes: `loadKakaoMapsSdk` (Task 4, `lib/kakaoMap.ts`), `RoutePoint`/`RestaurantResult` (Task 3, `lib/api.ts`)
- Produces: `MapViewProps { route: RoutePoint[]; restaurants: RestaurantResult[]; highlightedRestaurantId: string | null; center: { lat: number; lng: number } | null }`, 기본 export `MapView`

- [ ] **Step 1: 컴포넌트 구현**

`frontend/components/MapView.tsx`:
```tsx
"use client";

import { useEffect, useRef } from "react";
import { loadKakaoMapsSdk } from "../lib/kakaoMap";
import type { RestaurantResult, RoutePoint } from "../lib/api";

const DEFAULT_CENTER = { lat: 37.5665, lng: 126.978 }; // 서울시청

export interface MapViewProps {
  route: RoutePoint[];
  restaurants: RestaurantResult[];
  highlightedRestaurantId: string | null;
  center: { lat: number; lng: number } | null;
}

export default function MapView({ route, restaurants, highlightedRestaurantId, center }: MapViewProps) {
  const containerRef = useRef<HTMLDivElement>(null);
  const mapRef = useRef<any>(null);
  const kakaoRef = useRef<any>(null);
  const polylineRef = useRef<any>(null);
  const markersRef = useRef<Map<string, any>>(new Map());
  const infoWindowRef = useRef<any>(null);

  useEffect(() => {
    let cancelled = false;
    loadKakaoMapsSdk().then((kakao) => {
      if (cancelled || !containerRef.current) return;
      kakaoRef.current = kakao;
      const initialCenter = center ?? DEFAULT_CENTER;
      mapRef.current = new kakao.maps.Map(containerRef.current, {
        center: new kakao.maps.LatLng(initialCenter.lat, initialCenter.lng),
        level: 6,
      });
    });
    return () => {
      cancelled = true;
    };
    // 최초 마운트 시 한 번만 지도를 생성한다 — 이후 center 변경은 아래 별도 effect가 panTo로 처리한다.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  useEffect(() => {
    if (!mapRef.current || !kakaoRef.current || !center) return;
    mapRef.current.panTo(new kakaoRef.current.maps.LatLng(center.lat, center.lng));
  }, [center]);

  useEffect(() => {
    const kakao = kakaoRef.current;
    const map = mapRef.current;
    if (!kakao || !map) return;

    if (polylineRef.current) {
      polylineRef.current.setMap(null);
      polylineRef.current = null;
    }
    if (route.length > 0) {
      const path = route.map((p) => new kakao.maps.LatLng(p.lat, p.lng));
      polylineRef.current = new kakao.maps.Polyline({
        path,
        strokeWeight: 4,
        strokeColor: "#2563eb",
        strokeOpacity: 0.8,
      });
      polylineRef.current.setMap(map);
    }
  }, [route]);

  useEffect(() => {
    const kakao = kakaoRef.current;
    const map = mapRef.current;
    if (!kakao || !map) return;

    markersRef.current.forEach((marker) => marker.setMap(null));
    markersRef.current.clear();

    restaurants.forEach((restaurant) => {
      const marker = new kakao.maps.Marker({
        position: new kakao.maps.LatLng(restaurant.latitude, restaurant.longitude),
        map,
      });
      kakao.maps.event.addListener(marker, "click", () => {
        if (infoWindowRef.current) {
          infoWindowRef.current.close();
        }
        infoWindowRef.current = new kakao.maps.InfoWindow({
          content: `<div style="padding:8px;font-size:13px;">
            <strong>${restaurant.name}</strong><br/>
            ${restaurant.distance_from_route_km.toFixed(1)}km · ${Math.floor(restaurant.cumulative_time_sec / 60)}분 지점
          </div>`,
        });
        infoWindowRef.current.open(map, marker);
      });
      markersRef.current.set(restaurant.id, marker);
    });
  }, [restaurants]);

  useEffect(() => {
    if (!highlightedRestaurantId) return;
    const marker = markersRef.current.get(highlightedRestaurantId);
    const map = mapRef.current;
    if (!marker || !map) return;
    map.panTo(marker.getPosition());
  }, [highlightedRestaurantId]);

  return <div ref={containerRef} className="h-full min-h-[400px] w-full rounded" />;
}
```

- [ ] **Step 2: 타입 체크**

Run: `cd frontend && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add frontend/components/MapView.tsx
git commit -m "feat: add MapView component with route polyline and restaurant markers"
```

---

### Task 8: RestaurantList / RestaurantCard 컴포넌트

**Files:**
- Create: `frontend/components/RestaurantCard.tsx`
- Create: `frontend/components/RestaurantList.tsx`

**Interfaces:**
- Consumes: `formatDistance`/`formatDuration` (Task 3, `lib/format.ts`), `RestaurantResult` (Task 3, `lib/api.ts`)
- Produces: `RestaurantCardProps { restaurant: RestaurantResult; isHovered: boolean; onHover: (id: string | null) => void }`, `RestaurantListProps { restaurants: RestaurantResult[]; hoveredId: string | null; onHover: (id: string | null) => void }`, 각각 기본 export

**주의:** `RestaurantList`는 "매칭된 맛집이 0건"인 경우를 항상 빈 상태 문구로 렌더링한다 — "검색을 아직 안 함" 상태와는 구분되어야 하는데, 그 구분은 이 컴포넌트가 아니라 호출하는 쪽(Task 9의 `app/page.tsx`)이 담당한다: 검색 결과가 없을 때는 이 컴포넌트 자체를 렌더링하지 않고, 검색이 완료된 뒤에만 렌더링한다.

- [ ] **Step 1: RestaurantCard 구현**

`frontend/components/RestaurantCard.tsx`:
```tsx
"use client";

import { formatDistance, formatDuration } from "../lib/format";
import type { RestaurantResult } from "../lib/api";

export interface RestaurantCardProps {
  restaurant: RestaurantResult;
  isHovered: boolean;
  onHover: (id: string | null) => void;
}

export default function RestaurantCard({ restaurant, isHovered, onHover }: RestaurantCardProps) {
  return (
    <div
      onMouseEnter={() => onHover(restaurant.id)}
      onMouseLeave={() => onHover(null)}
      className={`rounded border p-3 ${isHovered ? "border-blue-500 bg-blue-50" : "border-gray-200"}`}
    >
      <div className="flex items-baseline justify-between">
        <span className="font-medium">{restaurant.name}</span>
        <span className="text-sm text-gray-500">{restaurant.category}</span>
      </div>
      <div className="mt-1 text-sm text-gray-600">
        {formatDistance(restaurant.distance_from_route_km)} · 출발 후 {formatDuration(restaurant.cumulative_time_sec)}
      </div>
      {restaurant.broadcasts.length > 0 && (
        <div className="mt-1 text-xs text-gray-500">{restaurant.broadcasts.join(", ")}</div>
      )}
      <div className="mt-1 text-xs text-gray-500">
        {restaurant.address}
        {restaurant.phone && ` · ${restaurant.phone}`}
        {restaurant.hours && ` · ${restaurant.hours}`}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: RestaurantList 구현**

`frontend/components/RestaurantList.tsx`:
```tsx
"use client";

import RestaurantCard from "./RestaurantCard";
import type { RestaurantResult } from "../lib/api";

export interface RestaurantListProps {
  restaurants: RestaurantResult[];
  hoveredId: string | null;
  onHover: (id: string | null) => void;
}

export default function RestaurantList({ restaurants, hoveredId, onHover }: RestaurantListProps) {
  if (restaurants.length === 0) {
    return <p className="p-4 text-center text-gray-500">이 경로 근처엔 방송 맛집이 없어요</p>;
  }

  return (
    <div className="flex flex-col gap-2 overflow-y-auto">
      {restaurants.map((restaurant) => (
        <RestaurantCard
          key={restaurant.id}
          restaurant={restaurant}
          isHovered={hoveredId === restaurant.id}
          onHover={onHover}
        />
      ))}
    </div>
  );
}
```

- [ ] **Step 3: 타입 체크**

Run: `cd frontend && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add frontend/components/RestaurantCard.tsx frontend/components/RestaurantList.tsx
git commit -m "feat: add RestaurantList and RestaurantCard components"
```

---

### Task 9: 메인 페이지 통합 (데이터 흐름 + 에러 처리 + 반응형 레이아웃)

**Files:**
- Modify: `frontend/app/page.tsx` (create-next-app이 생성한 기본 템플릿을 전체 교체)

**Interfaces:**
- Consumes: `fetchRouteRestaurants`/`ApiError`/`RouteRestaurantsResponse` (Task 3), `SearchForm`/`SelectedPlace` (Task 5), `FilterBar`/`Filters` (Task 6), `MapView` (Task 7), `RestaurantList` (Task 8)
- Produces: 없음 (최종 페이지, 다른 태스크가 의존하지 않음)

이 태스크가 이 계획의 유일한 "실제 브라우저 골든 패스 확인" 지점이다(설계 문서 §8) — Task 4~8은 타입 체크로만 검증했으므로, 여기서 처음으로 전체 기능을 눈으로 확인한다.

- [ ] **Step 1: 페이지 구현**

`frontend/app/page.tsx`를 다음으로 전체 교체한다:
```tsx
"use client";

import { useState } from "react";
import SearchForm, { type SelectedPlace } from "../components/SearchForm";
import FilterBar, { type Filters } from "../components/FilterBar";
import MapView from "../components/MapView";
import RestaurantList from "../components/RestaurantList";
import { ApiError, fetchRouteRestaurants, type RouteRestaurantsResponse } from "../lib/api";

function errorMessageFor(error: unknown): string {
  if (error instanceof ApiError) {
    if (error.status === 0) return "서버에 연결할 수 없습니다";
    if (error.status === 500) return "일시적인 오류입니다, 잠시 후 다시 시도해주세요";
    if (error.status === 502) return "경로를 가져오지 못했습니다, 다시 시도해주세요";
    return "요청 중 오류가 발생했습니다";
  }
  return "알 수 없는 오류가 발생했습니다";
}

export default function Home() {
  const [origin, setOrigin] = useState<SelectedPlace | null>(null);
  const [destination, setDestination] = useState<SelectedPlace | null>(null);
  const [filters, setFilters] = useState<Filters>({ broadcast: "", category: "" });
  const [result, setResult] = useState<RouteRestaurantsResponse | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  const [errorMessage, setErrorMessage] = useState<string | null>(null);
  const [hoveredId, setHoveredId] = useState<string | null>(null);
  const [mapCenter, setMapCenter] = useState<{ lat: number; lng: number } | null>(null);

  async function runSearch(
    searchOrigin: SelectedPlace,
    searchDestination: SelectedPlace,
    searchFilters: Filters
  ) {
    setIsLoading(true);
    setErrorMessage(null);
    try {
      const response = await fetchRouteRestaurants({
        originLat: searchOrigin.lat,
        originLng: searchOrigin.lng,
        destinationLat: searchDestination.lat,
        destinationLng: searchDestination.lng,
        broadcast: searchFilters.broadcast || undefined,
        category: searchFilters.category || undefined,
      });
      setResult(response);
    } catch (error) {
      setErrorMessage(errorMessageFor(error));
    } finally {
      setIsLoading(false);
    }
  }

  function handleSearch(searchOrigin: SelectedPlace, searchDestination: SelectedPlace) {
    setOrigin(searchOrigin);
    setDestination(searchDestination);
    runSearch(searchOrigin, searchDestination, filters);
  }

  function handleFiltersChange(newFilters: Filters) {
    setFilters(newFilters);
    if (origin && destination) {
      runSearch(origin, destination, newFilters);
    }
  }

  function handleOriginSelect(place: SelectedPlace) {
    setMapCenter({ lat: place.lat, lng: place.lng });
  }

  return (
    <main className="flex h-screen flex-col gap-4 p-4">
      <div className="flex flex-col gap-2">
        <SearchForm onOriginSelect={handleOriginSelect} onSearch={handleSearch} isLoading={isLoading} />
        <FilterBar filters={filters} onChange={handleFiltersChange} />
      </div>

      {errorMessage && <p className="text-sm text-red-600">{errorMessage}</p>}

      <div className="flex flex-1 flex-col gap-4 overflow-hidden sm:flex-row">
        <div className="sm:w-2/3">
          <MapView
            route={result?.route.points ?? []}
            restaurants={result?.restaurants ?? []}
            highlightedRestaurantId={hoveredId}
            center={mapCenter}
          />
        </div>
        <div className="sm:w-1/3 sm:overflow-y-auto">
          {result ? (
            <RestaurantList restaurants={result.restaurants} hoveredId={hoveredId} onHover={setHoveredId} />
          ) : (
            <p className="p-4 text-center text-gray-500">출발지와 도착지를 검색해주세요</p>
          )}
        </div>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: 타입 체크 + 프로덕션 빌드**

Run: `cd frontend && npx tsc --noEmit && npm run build`
Expected: 둘 다 에러 없이 통과

- [ ] **Step 3: 백엔드 개발 서버 실행 (별도 터미널)**

```bash
cd backend
# Windows PowerShell:
$env:KAKAO_REST_API_KEY="<실제 카카오 REST API 키>"
$env:DATABASE_URL="sqlite:///./foodmap.db"
$env:FRONTEND_ORIGINS="http://localhost:3000"
uvicorn app.api.main:app --reload
```

- [ ] **Step 4: 프론트엔드 개발 서버 실행**

```bash
cd frontend
npm run dev
```
`frontend/.env.local`에 `NEXT_PUBLIC_KAKAO_JS_KEY`(실제 키)와 `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`이 채워져 있는지 먼저 확인한다(Task 2에서 설정함).

- [ ] **Step 5: 브라우저 골든 패스 확인**

`http://localhost:3000`을 열고 다음을 직접 확인한다:

1. 출발지 입력창에 "서울역" 입력 → 자동완성 드롭다운이 뜨는지 확인 → 하나 선택 → 지도 중심이 그 위치로 이동하는지 확인
2. 도착지 입력창에 "강남역" 입력 → 선택
3. "검색" 버튼 클릭 → 버튼이 "검색 중..."으로 바뀌는지 확인 → 완료 후 지도에 경로선과 맛집 마커, 오른쪽(모바일이면 아래쪽)에 리스트가 뜨는지 확인
4. 마커를 클릭하면 인포윈도우(상호명/거리/시간)가 뜨는지 확인
5. 리스트 항목에 마우스를 올리면 지도가 해당 마커 쪽으로 이동(패닝)하는지 확인
6. 방송프로그램 드롭다운에서 하나를 선택 → 결과가 그 프로그램 맛집만으로 바뀌는지 확인
7. 업종 입력창에 "한식"을 입력 → 결과가 갱신되는지 확인
8. 업종 입력창에 실제로 없을 법한 값(예: "존재하지않는업종")을 입력 → "이 경로 근처엔 방송 맛집이 없어요" 빈 상태 문구가 뜨는지 확인 (에러 메시지가 아니라 빈 상태여야 함)
9. 브라우저 개발자도구에서 모바일 너비로 좁혀서, 지도/리스트가 좌우 분할에서 상하 배치로 바뀌는지 확인

- [ ] **Step 6: 에러 처리 확인**

Step 3의 백엔드 서버를 `Ctrl+C`로 멈춘 뒤, 프론트엔드에서 다시 검색을 시도한다.
Expected: "서버에 연결할 수 없습니다" 메시지가 표시됨 (충돌 없이).

- [ ] **Step 7: 커밋**

```bash
git add frontend/app/page.tsx
git commit -m "feat: wire SearchForm, FilterBar, MapView, and RestaurantList into the main page"
```
